# 快速启动指南

本指南将帮助您快速启动酒店大堂无人售货超市系统。

## 📋 前置条件

确保您的开发环境已安装以下软件：

- ☑️ JDK 17 或更高版本
- ☑️ Maven 3.6+ 
- ☑️ MySQL 8.0+
- ☑️ Redis 6.0+
- ☑️ Node.js 18+
- ☑️ npm 9+

## 🚀 启动步骤

### 第一步：数据库准备

1. **启动MySQL服务**
```bash
# Linux/Mac
sudo systemctl start mysql
# 或
mysql.server start

# Windows
net start MySQL80
```

2. **创建数据库并导入数据**
```bash
# 登录MySQL
mysql -u root -p

# 执行数据库脚本
source /path/to/HOTEL_POJO/backend/src/main/resources/db.sql

# 或使用命令行直接导入
mysql -u root -p < backend/src/main/resources/db.sql
```

3. **验证数据库**
```sql
USE hotel_store;
SHOW TABLES;
-- 应该看到8个表：sys_user, product, cart, orders, order_item, coupon, user_coupon, banner

SELECT * FROM sys_user WHERE username = 'admin';
-- 应该看到默认管理员账户
```

### 第二步：启动Redis

```bash
# Linux/Mac
redis-server

# Windows
# 直接运行 redis-server.exe

# 验证Redis是否启动
redis-cli ping
# 应该返回 PONG
```

### 第三步：配置后端

1. **检查配置文件** `backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_store?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root  # 修改为您的MySQL密码
  data:
    redis:
      host: localhost
      port: 6379
```

2. **修改数据库密码**（如果需要）
```bash
# 编辑配置文件
nano backend/src/main/resources/application.yml
# 或
vim backend/src/main/resources/application.yml
```

### 第四步：启动后端

```bash
# 进入后端目录
cd backend

# 安装依赖并启动
mvn clean install
mvn spring-boot:run

# 等待看到以下输出：
# =================================
# 酒店无人售货超市系统启动成功！
# 接口文档地址: http://localhost:8080/doc.html
# =================================
```

### 第五步：启动前端

打开**新的终端窗口**：

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev

# 等待看到：
# VITE v5.x.x  ready in xxx ms
# ➜  Local:   http://localhost:3000/
```

### 第六步：访问系统

1. **前端界面**: http://localhost:3000
2. **API文档**: http://localhost:8080/doc.html

## 👤 登录系统

使用默认管理员账户：
- **用户名**: `admin`
- **密码**: `123456`

## ✅ 验证功能

### 前端验证清单

- [ ] 访问首页，看到轮播图和商品列表
- [ ] 右上角显示用户头像（admin）
- [ ] 点击语言切换按钮（地球图标），切换中英文
- [ ] 点击商品卡片，进入商品详情页
- [ ] 在商品详情页点击"加入购物车"
- [ ] 查看购物车（右上角购物车图标有徽章数字）
- [ ] 在购物车中调整数量，点击结算
- [ ] 查看订单列表
- [ ] 访问优惠券中心，领取优惠券
- [ ] 点击"扫码"按钮，测试摄像头调用

### 后端验证清单

访问 http://localhost:8080/doc.html

- [ ] 测试用户登录接口
- [ ] 测试商品列表接口
- [ ] 测试购物车接口
- [ ] 测试订单创建接口
- [ ] 测试优惠券领取接口

## 🐛 常见问题

### 问题1：后端启动失败 - 数据库连接错误

**错误信息**: `Communications link failure`

**解决方案**:
1. 确认MySQL服务已启动
2. 检查用户名和密码是否正确
3. 检查数据库`hotel_store`是否已创建

### 问题2：后端启动失败 - Redis连接错误

**错误信息**: `Unable to connect to Redis`

**解决方案**:
1. 确认Redis服务已启动：`redis-cli ping`
2. 检查Redis端口是否为6379
3. 如使用Docker运行Redis，确保端口映射正确

### 问题3：前端无法访问后端API

**错误信息**: `Network Error` 或 `CORS Error`

**解决方案**:
1. 确认后端已启动在8080端口
2. 检查后端日志是否有CORS相关错误
3. 清除浏览器缓存并刷新

### 问题4：npm install失败

**错误信息**: 依赖下载失败

**解决方案**:
```bash
# 清除npm缓存
npm cache clean --force

# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### 问题5：摄像头无法打开

**原因**: 浏览器安全策略

**解决方案**:
1. 使用HTTPS或localhost访问
2. 在浏览器设置中允许摄像头权限
3. 检查电脑是否有摄像头设备

## 📦 生产部署

### 后端部署

```bash
# 构建JAR包
cd backend
mvn clean package -DskipTests

# 运行JAR包
java -jar target/hotel-store-1.0.0.jar
```

### 前端部署

```bash
# 构建生产版本
cd frontend
npm run build

# 部署dist目录到Nginx或其他Web服务器
```

## 🔧 开发模式

### 后端热部署

在IDEA中:
1. 安装Spring Boot DevTools
2. 启用自动构建
3. 修改代码后自动重启

### 前端热更新

Vite自动支持热模块替换(HMR)，修改代码后自动刷新。

## 📞 获取帮助

遇到问题？

1. 查看项目README.md
2. 查看后端README.md (backend/README.md)
3. 查看前端README.md (frontend/README.md)
4. 提交Issue到GitHub仓库

---

**祝您使用愉快！** 🎉
